
services:
  - type: web
    name: neiro-chat
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn chat_server:app --host 0.0.0.0 --port 10000
    plan: free
    branch: main
    healthCheckPath: /
